<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admission Portal</title>

    <!-- Favicon with correct path -->
    <link rel="icon" href="/images/logo.png" sizes="48x48" /> <!-- Ensure this is the correct path -->
    <!-- CSS Includes -->
    <link rel="stylesheet" href="~/lib/bootstrap/dist/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" /> <!-- Font Awesome -->
    <link rel="stylesheet" href="~/css/site.css" asp-append-version="true" />
    <link rel="stylesheet" href="~/system_SIS.styles.css" asp-append-version="true" />

    <style>
        body {
            position: relative; /* Required for positioning the pseudo-element */
            background-image: url('/images/bgjr.jpg'); /* Adjusted path */
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            padding: 20px;
            border-radius: 10px;
            color: #172154; /* Dark blue for normal text */
            font-family: 'Poppins', sans-serif; /* Apply Poppins font */
        }

            body::before {
                content: ""; /* Required for the pseudo-element */
                position: absolute;
                top: 0;
                left: 0;
                width: 100%;
                height: 100%;
                background: rgba(128, 128, 128, 0.7); /* Gray with 70% opacity */
                z-index: -1; /* Ensures the overlay is behind the content */
            }
    </style>
</head>
<body>
    <!-- Your content here -->
</body>
</html>
